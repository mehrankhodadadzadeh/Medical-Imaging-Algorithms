# Data

Course-provided medical-image files and phantoms are intentionally not included in this repository.

The notebooks expect locally available input files such as MRI images (`.mha`), a CT sinogram (`.bin`), and PET activity phantoms (`.npy`). Use only data that you are authorized to access and redistribute.
